import * as React from "react";

// eslint-disable-next-line react/require-default-props
const MappingsForm = ({ namespace }: { namespace: string }) => (
  <div>{namespace}</div>
);

export default MappingsForm;
