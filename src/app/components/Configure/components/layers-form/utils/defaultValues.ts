import { LayersMapping } from "../../mappings-form/utils/types";
import FieldNames from "../../mappings-form/utils/fieldNames";

const defaultValues: LayersMapping = {
  [FieldNames.NAME]: "New layer class",
  [FieldNames.MAPPING]: [],
};

export default defaultValues;
